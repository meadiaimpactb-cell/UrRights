# النشر — حقوقك

## المسار السريع: ارفع `dist` مباشرة

ملف `dist/boot.js` **مبنيّ ومكتفٍ ذاتيًا** — كل المكتبات مدمجة داخله، ولا يحتاج `node_modules` على الخادم إطلاقًا.

ارفع هذين فقط:

```
dist/boot.js
dist/public/
```

ومعهما ملف `.env` بجانبهما، ثم:

```bash
NODE_ENV=production node boot.js
```

الخادم يقدّم الموقع والـ API على نفس المنفذ (`PORT`، الافتراضي 3000).

### للإبقاء عليه يعمل

```bash
npm install -g pm2
pm2 start boot.js --name urrights
pm2 save && pm2 startup
```

---

## قبل أول تشغيل: قاعدة البيانات

تحتاج MySQL 8 فعلية. أنشئها من لوحة الاستضافة، ثم على جهازك:

```bash
nvm use 20
npm ci
# ضع DATABASE_URL في .env
npm run db:push          # ينشئ الجداول
npm run db:seed          # يعبّئ العربية والإنجليزية
npm run db:seed:translate # اختياري: بقية اللغات ترجمة آلية
```

`db:push` و`db:seed` يتصلان بقاعدة البيانات مباشرة، فيمكن تشغيلهما من جهازك ما دام الاتصال الخارجي مسموحًا في الاستضافة، أو من الخادم بعد `npm ci` هناك.

---

## ملف `.env`

```ini
DATABASE_URL=mysql://user:pass@host:3306/urrights
NODE_ENV=production
PORT=3000

# مطلوب حتى يصبح أول داخل مديرًا
OWNER_UNION_ID=

# مصادقة Kimi — انظر التحذير أدناه
VITE_KIMI_AUTH_URL=
KIMI_API_BASE=
```

---

## ⚠️ لوحة التحكم لن تعمل بعد النشر

تسجيل الدخول مربوط بمنصة Kimi عبر `VITE_KIMI_AUTH_URL`. خارج تلك المنصة **لا يستطيع أحد الدخول للوحة التحكم** — الموقع العام يعمل، لكن:

- لا أحد يقرأ الدردشات الواردة
- لا أحد يرى طلبات المساعدة
- لا يمكن تعديل أي نص

**لا تُطلق الموقع للجمهور قبل حل هذا.** موقع يَعِد بالمساندة ولا أحد يرد فيه أسوأ من عدم وجوده.

الحل: استبدال المصادقة بدخول بالبريد وكلمة المرور من قاعدة البيانات، أو بمزوّد OAuth عام.

---

## إعادة البناء عند أي تعديل

```bash
nvm use 20        # مهم — لا تستخدم Node 22
npm ci
npm run build
```

ثم ارفع `dist` من جديد.
